fnc print(a : str, ...) : void;

struct employee {
	salary: i32,
	name : str,
	id : i32,
};

fnc main() : i32 {
	john : employee;
	john.name = "John";
	john.id = 22;
	john.salary = 3400;

	print("Name: %s\nid: %d\nsalary : %d\n", john.name, john.id, john.salary);
	ret 0;
}
